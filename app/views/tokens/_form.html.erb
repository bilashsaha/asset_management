<%= form_for(@token) do |f| %>
  <% if @token.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@token.errors.count, "error") %> prohibited this token from being saved:</h2>

      <ul>
      <% @token.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sanchaypatra_id %><br>
    <%= f.number_field :sanchaypatra_id %>
  </div>
  <div class="field">
    <%= f.label :token_number %><br>
    <%= f.number_field :token_number %>
  </div>
  <div class="field">
    <%= f.label :token_date %><br>
    <%= f.datetime_select :token_date %>
  </div>
  <div class="field">
    <%= f.label :is_redeemed %><br>
    <%= f.check_box :is_redeemed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
